import AuthorizationButtons from './authorizationButtons';

const AuthorizationButtonsContainer: React.FunctionComponent = () =>
    <AuthorizationButtons onLoginClick={() => null} onRegistrationClick={() => null} />;

export default AuthorizationButtonsContainer;
