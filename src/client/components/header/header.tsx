import './header.scss';

const Header: React.FunctionComponent = () => (
    <header
        className='header'
    >
        <div>logo</div>
        <div>form</div>
    </header>
);

export default Header;
